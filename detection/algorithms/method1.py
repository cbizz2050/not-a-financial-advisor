class Method1Detector:
    def __init__(self, pattern_params):
        self.pattern_params = pattern_params

    def detect_pattern(self, detection_event):
        # Perform pattern detection on the detection event

        # Calculate the confidence rating for the detected pattern
        confidence_rating = 0.8

        return confidence_rating
